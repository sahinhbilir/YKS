{
  "name": "yks-tekrar-firestore-rules-test",
  "private": true,
  "description": "Reproduces the Firebase Emulator Suite security-rule checks for ogrenciler/{syncId} against ../../firestore.rules. No real Firebase project or credentials needed.",
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --project demo-yks-test \"node test/firebase/firestore.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.2",
    "firebase": "^12.18.0",
    "firebase-tools": "^15.29.0"
  }
}
